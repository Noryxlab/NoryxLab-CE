apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: noryx-ce
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: noryx-ce
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      imagePullSecrets:
        - name: harbor-regcred
      containers:
        - name: postgres
          image: harbor.lan/noryx-ce/postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: noryx
            - name: POSTGRES_USER
              value: noryx
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: noryx-secrets
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: postgres-data
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: noryx-ce
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
