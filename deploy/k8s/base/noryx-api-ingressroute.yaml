apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: noryx-https-redirect
  namespace: noryx
spec:
  redirectScheme:
    scheme: https
    permanent: true
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: noryx-security-headers
  namespace: noryx
spec:
  headers:
    stsSeconds: 31536000
    stsIncludeSubdomains: true
    stsPreload: true
    forceSTSHeader: true
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: noryx-http-redirect
  namespace: noryx
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`datalab.example.local`)
      kind: Rule
      middlewares:
        - name: noryx-https-redirect
      services:
        - name: noryx-frontend
          port: 80
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: noryx-backend
  namespace: noryx
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`datalab.example.local`) && PathPrefix(`/auth`)
      kind: Rule
      middlewares:
        - name: noryx-security-headers
      services:
        - name: keycloak
          port: 8080
    - match: Host(`datalab.example.local`) && (PathPrefix(`/api`) || PathPrefix(`/swagger`) || PathPrefix(`/workspaces`) || PathPrefix(`/apps`) || Path(`/healthz`))
      kind: Rule
      middlewares:
        - name: noryx-security-headers
      services:
        - name: noryx-backend
          port: 8080
    - match: Host(`datalab.example.local`) && PathPrefix(`/`)
      kind: Rule
      middlewares:
        - name: noryx-security-headers
      services:
        - name: noryx-frontend
          port: 80
  tls:
    certResolver: le
